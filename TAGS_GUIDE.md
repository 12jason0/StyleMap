# 코스 Tags 작성 가이드

## 📋 Onboarding 질문 ↔ Course Tags 매칭 관계

### 1️⃣ **companionType** → `target` 태그

| Onboarding 값 | Course tags.target에 들어갈 값 |
|--------------|-------------------------------|
| `solo` | "혼자", "솔로" |
| `couple` | "커플", "연인", "데이트" |
| `friends` | "친구", "소그룹" |
| `family` | "가족" |
| `blinddate` | "소개팅" |

**예시:**
```json
{
  "target": ["커플", "데이트"]  // couple 선택 시 매칭됨
}
```

---

### 2️⃣ **vibe** → `mood` 태그

| Onboarding 값 | Course tags.mood에 들어갈 값 |
|--------------|----------------------------|
| `romantic` | "로맨틱", "감성", "분위기" |
| `calm` | "조용한", "잔잔한", "힐링" |
| `hip` | "트렌디한", "핫플", "인스타" |
| `private` | "프라이빗", "조용한" |
| `active` | "액티브", "활동적인", "모험" |

**예시:**
```json
{
  "mood": ["조용한", "트렌디한"]  // calm 또는 hip 선택 시 매칭됨
}
```

---

### 3️⃣ **budgetRange** → `budget` 태그

| Onboarding 값 | Course tags.budget 값 |
|--------------|---------------------|
| `budget` | "budget" |
| `moderate` | "moderate" |
| `premium` | "premium" |
| `luxury` | "luxury" |

**예시:**
```json
{
  "budget": "moderate"  // moderate 선택 시 정확히 매칭됨
}
```

---

### 4️⃣ **specialDateType** → `target` 태그 (추가)

| Onboarding 값 | Course tags.target에 들어갈 값 |
|--------------|-------------------------------|
| `100days` | "100일", "백일" |
| `200days` | "200일", "이백일" |
| `proposal` | "프로포즈", "청혼" |
| `birthday` | "생일", "생신" |
| `anniversary` | "기념일", "애니버서리" |
| `other` | "기타" |

**예시:**
```json
{
  "target": ["커플", "100일"]  // specialDateType이 "100days"일 때 매칭됨
}
```

---

## 🎯 추가 Tags (Onboarding에는 없지만 코스에 포함 가능)

### `time` 태그
- "아침", "오전", "오후", "저녁", "밤" 등
- 코스가 추천되는 시간대

### `theme` 태그
- "카페투어", "맛집", "쇼핑", "산책", "갤러리", "브런치" 등
- 코스의 테마/활동 유형

### `concept` 태그
- "감성", "데이트", "인생샷", "핫플", "트렌디", "인더스트리얼" 등
- 코스의 컨셉/키워드

---

## ✅ 완전한 Tags 예시

```json
{
  "target": ["커플", "데이트", "100일"],      // companionType: couple, specialDateType: 100days
  "mood": ["조용한", "트렌디한", "예술적인"],  // vibe: calm 또는 hip
  "budget": "moderate",                       // budgetRange: moderate
  "time": ["오후", "저녁"],                   // 추가 정보
  "theme": ["카페투어", "맛집", "갤러리"],     // 추가 정보
  "concept": ["감성", "데이트", "인더스트리얼"] // 추가 정보
}
```

---

## ⚠️ 주의사항

1. **target**: companionType과 specialDateType 모두 `target` 배열에 포함 가능
2. **mood**: 여러 개 선택 가능 (배열)
3. **budget**: 정확히 일치해야 함 (문자열)
4. **time, theme, concept**: Onboarding에는 없지만 코스 특성을 더 잘 표현할 수 있음

